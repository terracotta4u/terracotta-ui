{
  "name": "terracotta-ui",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "mkdir -p dist && lightningcss --bundle terracotta-ui/terracotta.css -o dist/terracotta.css"
  },
  "devDependencies": {
    "lightningcss-cli": "^1.30.1",
    "next": "^15.5.25"
  }
}